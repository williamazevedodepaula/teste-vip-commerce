{
  "db": {
    "host": "localhost",
    "port": 3306,
    "url": "mysql://root:1q2w3e4r@database/vipdb",
    "database": "${DB_NAME}",
    "password": "${DB_PASSWORD}",
    "name": "db",
    "user": "${DB_USERNAME}",
    "connector": "mysql"
  },
  "db2": {
    "host": "localhost",
    "name": "db2",
    "connector": "memory"
  },
  "mail": {
    "name": "mail",
    "connector": "mail",
    "transports": [
      {
        "type": "SMTP",
        "host": "${SMTP_SERVER}",
        "secure": true,
        "port": "${SMTP_PORT}",
        "auth": {
          "user": "${SMTP_USERNAME}",
          "pass": "${SMTP_PASSWORD}"
        }
      }
    ]
  }
}
